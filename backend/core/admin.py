"""
Core app admin configuration.

The core app contains abstract models and utilities, so no models 
need to be registered here.
"""
from django.contrib import admin

# Core app has no models to register - it provides abstract base classes

